# 3c-clock-carousel
A 3C-branded web tool that animates uploaded card images in a vertical clock-rotation carousel and exports the result as WebM for video production. Vanilla HTML/CSS/JS. No dependencies. No backend.
